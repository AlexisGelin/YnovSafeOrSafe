using System.Collections;
using System.Collections.Generic;
using UnityEditor.EditorTools;
using UnityEngine;

public class CloudGenerator : MonoBehaviour
{
    private void Start()
    {
/*        var result = PoolManager.Instance.gameobjectPoolDictionary.ContainsKey("Clouds");
        PoolManager.Instance.gameobjectPoolDictionary[].Release(pooledCollectible);*/
    }
}
